import 'package:flutter/material.dart';

class BottomLocationLayout extends StatelessWidget {
  const BottomLocationLayout({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class CategorySelectionLayout extends StatelessWidget {
  final dynamic isOffer;
  const CategorySelectionLayout({super.key, this.isOffer});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class ZoneDropDown extends StatelessWidget {
  final dynamic isAddLocation;
  const ZoneDropDown({super.key, this.isAddLocation});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class CompanyTopLayout extends StatelessWidget {
  const CompanyTopLayout({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class ContactUsLayout extends StatelessWidget {
  const ContactUsLayout({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class HomeJobRequestDetails extends StatelessWidget {
  const HomeJobRequestDetails({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class LocationTextFieldLayout extends StatelessWidget {
  const LocationTextFieldLayout({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class OfferServiceSelectionLayout extends StatelessWidget {
  const OfferServiceSelectionLayout({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class PendingApprovalBillSummary extends StatelessWidget {
  final dynamic title;
  final dynamic status;
  final dynamic bookingModel;
  const PendingApprovalBillSummary({super.key, this.title, this.status, this.bookingModel});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class ServiceDetailShimmer extends StatelessWidget {
  const ServiceDetailShimmer({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class ServicemenDetailProfileLayout extends StatelessWidget {
  const ServicemenDetailProfileLayout({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class ZoneBottomSheet extends StatelessWidget {
  final dynamic isAddLocation;
  const ZoneBottomSheet({super.key, this.isAddLocation});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class KnownLanguageBottomSheet extends StatelessWidget {
  const KnownLanguageBottomSheet({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class ThemeSelect extends StatelessWidget {
  const ThemeSelect({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class PageDetail extends StatelessWidget {
  final dynamic data;
  final dynamic index;
  final dynamic page;
  const PageDetail({super.key, this.data, this.index, this.page});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class BidAmountSheet extends StatelessWidget {
  final dynamic serviceId;
  final dynamic bookingId;
  const BidAmountSheet({super.key, this.serviceId, this.bookingId});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class SelectOptionLayout extends StatelessWidget {
  final dynamic data;
  final dynamic index;
  final dynamic list;
  final dynamic onTap;
  const SelectOptionLayout({super.key, this.data, this.index, this.list, this.onTap});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class AppDetailsLayout extends StatelessWidget {
  final dynamic data;
  final dynamic index;
  final dynamic list;
  final dynamic onTap;
  const AppDetailsLayout({super.key, this.data, this.index, this.list, this.onTap});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class ListTileLayout extends StatelessWidget {
  final dynamic data;
  final dynamic index;
  final dynamic list;
  final dynamic selectedCategory;
  final dynamic isAddService;
  final dynamic onTap;
  const ListTileLayout({super.key, this.data, this.index, this.list, this.selectedCategory, this.isAddService, this.onTap});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class PaymentSummaryWidget extends StatelessWidget {
  final dynamic title;
  final dynamic status;
  final dynamic data;
  final dynamic booking;
  const PaymentSummaryWidget({super.key, this.title, this.status, this.data, this.booking});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class PendingApprovalBillSummaryLayout extends StatelessWidget {
  final dynamic title;
  final dynamic status;
  const PendingApprovalBillSummaryLayout({super.key, this.title, this.status});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class FeaturedServicesLayout extends StatelessWidget {
  final dynamic data;
  final dynamic index;
  final dynamic list;
  final dynamic onToggle;
  final dynamic onTap;
  const FeaturedServicesLayout({super.key, this.data, this.index, this.list, this.onToggle, this.onTap});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class TopCategoriesLayout extends StatelessWidget {
  final dynamic data;
  final dynamic index;
  final dynamic list;
  final dynamic rPadding;
  final dynamic isCategories;
  final dynamic selectedIndex;
  final dynamic onTap;
  const TopCategoriesLayout({super.key, this.data, this.index, this.list, this.rPadding, this.isCategories, this.selectedIndex, this.onTap});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class ChatLayout extends StatelessWidget {
  final dynamic data;
  final dynamic index;
  final dynamic isSentByMe;
  final dynamic document;
  final dynamic isEmailOrPhone;
  const ChatLayout({super.key, this.data, this.index, this.isSentByMe, this.document, this.isEmailOrPhone});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class AssignSingleServiceman extends StatelessWidget {
  final dynamic selectService;
  final dynamic data;
  const AssignSingleServiceman({super.key, this.selectService, this.data});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class AssignMultipleServiceman extends StatelessWidget {
  final dynamic selectService;
  final dynamic data;
  const AssignMultipleServiceman({super.key, this.selectService, this.data});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class BookingServicemenListFilter extends StatelessWidget {
  const BookingServicemenListFilter({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}

class UpdateBillSummaryBottom extends StatelessWidget {
  final dynamic data;
  const UpdateBillSummaryBottom({super.key, this.data});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}



